import Chai from './chaiReady';

function App() {
  return (
    <>
    <h1>Heyy itz Bkkyy</h1>
    <Chai/>
    </>
      
      /*  We can return anythng from different app by in this app , like we have to return text node 'here is chai ' by creating it in another jsx file then first create anothre app return the textnode to this jsx file  */
      /*We can only return a single elemwnt at a time so if there are multiple element we have to wrap them in single element like we will wrap them in a single div  */
  );
}
export default App
